<div class="site-headers">
  <h1>Formation</h1>
  <h4>Create a Format<span class="logo-color">i</span>on to Upload Plays</h4>
</div>

<div class="edit-container">
  <%= form_for(@formation) do |f| %>
    <% if @formation.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@formation.errors.count, "error") %> prohibited this formation from being saved:</h2>

        <ul>
        <% @formation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name, placeholder: "Formation" %>
    </div>
    <div class="field">
      <%= f.label :pdf_file %><br>
      <%= f.file_field :pdf_file %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
</div>