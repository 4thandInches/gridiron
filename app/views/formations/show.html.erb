<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @formation.name %>
</p>

<% @plays.each do |p| %>
  <p>
    <%= p.name %>
    <%= link_to "Show Play", p.pdf_file.url %>
  </p>
<% end %>



<%= link_to 'Edit', edit_formation_path(@formation) %> |
<%= link_to 'Back', formations_path %>
